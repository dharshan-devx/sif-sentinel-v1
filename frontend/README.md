# SIF SENTINEL Frontend

This directory is the designated location for the SIF SENTINEL web UI (Next.js + TypeScript).

## Boundaries
- The frontend must communicate with the backend exclusively through documented REST APIs.
- The frontend must **not** directly access PostgreSQL.
- The frontend must **not** import backend Python modules.
